using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Card_Move : Card
{
    public Card_Move(byte id) :base (id)
    {

        Use();
    }

    //needs to have tiles / properties and movement finished to finish making

    public override void Use()
    {

        //Player.Move(Player.playerPosition - 3);
    }
}
